name: Test runner
on:
  push:
    branches: [ main ]   # start pipeline als je naar main pusht

jobs:
  test:
    runs-on: self-hosted   # belangrijk: dit matcht jouw EC2 runner
    steps:
      - run: echo "Hello from my EC2 runner!"
